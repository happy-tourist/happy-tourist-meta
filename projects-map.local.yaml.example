# Локальные оверрайды путей из docs/projects-map.md
# Скопируйте в projects-map.local.yaml (файл в git не коммитится).
# Ключи — из таблицы «Пути для OpenSpec / агентов».
# Значения — относительные от корня happy-tourist-meta или абсолютные.

client: ../happy-tourist.github.io
server: ../happy-tourist-server
